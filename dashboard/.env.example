# API base URL — used for Vite dev proxy target and direct API calls.
# Default proxy target: http://127.0.0.1:8000 (must match SEEJOB_PORT in repo .env)
# VITE_API_URL=http://127.0.0.1:8000
#
# If the dashboard shows 502 on /api/*, the API is not running — start it with `seejob`.
